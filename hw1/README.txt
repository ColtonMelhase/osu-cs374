To compile the main.c file, run the command

    gcc --std=gnu99 -o movies.exe main.c

Then to run the program, using a file called movies_sample_1.csv as an example,
run the command

    movies.exe movies_sample_1.csv